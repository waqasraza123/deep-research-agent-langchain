# Evaluation Lab Baselines

Baselines are compact snapshots promoted from reviewed benchmark or quality gate runs.

Use them to compare future Evaluation Lab runs against known-good behavior. Baselines store scores, pass/fail state, missed traps, warning fingerprints, and compact artifact fingerprints. They intentionally avoid full artifacts and secrets.

Promote baselines deliberately after reviewing `governance_summary.md`.
